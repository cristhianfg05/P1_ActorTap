import java.util.Queue;

public class RingActor implements ActorInterface{

    /**TODOS LOS ACTORES TIENEN SU COLA**/
    private Queue<Message> queueMsg;
    @Override
    public void send(Message message) {
        /**IMPLEMENTAR RUNABLE + COLA CON MENSAJES**/
    }
}
